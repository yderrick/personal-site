---
title: 'Started over, and kept the code'
date: 2026-08-11
tags: [worldbuilder, refactor]
draft: false
backfilled: true
---

Reimported the whole codebase into a new repository as Worldbuilder Engine. Same code, clean history,
and a set of written conventions this time instead of whatever I remembered doing last month.

Two changes on top of the import. The Beats Checklist came out and a structured Scene/Sequel model
went in — the checklist was a list of things to tick, which told me nothing about whether a scene
actually worked. Scene/Sequel is a real structural model with opinions, so it can be wrong, which
makes it useful.

Then a fix for the scene modal's scroll and drag-and-drop reordering for chapters.

The restart is what got the project moving again. Not new code — a decision about how I was going to
work on it.
